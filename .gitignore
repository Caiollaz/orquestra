# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# estado do orquestra por-projeto: worktrees e quadro dos agentes são voláteis.
# Papéis e contextos são conteúdo autoral (as regras que os agentes recebem) →
# versionados, senão cada clone do repo começa sem contexto nenhum.
.orquestra/*
!.orquestra/roles/
!.orquestra/contexts/

# skills instaladas via npx skills (tooling, não produto)
.agents/
.claude/
skills-lock.json
